[package]
name = "relay"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["alloc"] }
toml = "0.8"
chrono = "0.4"
